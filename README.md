# Introduction

A CLI LLM coding agent using the Google Gemini API, written in Go.

This is currently a work in progress. So far, the program is able to
read and understand what its own `main.go` file does.
